<section
  id="portfolio"
  class="clients-served"
>
  <h3>Portfolio</h3>
  <div class="container">
    {% for project in projects %}
    <a href="">
      <div
        class="img"
        t
        style="background-image: url('{{project.img}}')"
      ></div>
      <div class="overlay">
        <h4>{{project.name}}</h4>
        <p>{{project.description}}</p>
      </div>
    </a>
    {% endfor %}
  </div>
</section>
